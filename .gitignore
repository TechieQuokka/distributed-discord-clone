# backend (rust) — 독립 crate마다 target이 있으므로 모든 target 제외 (R7)
target/
**/*.rs.bk
# frontend (web)
/frontend/node_modules
/frontend/dist
# 첨부 blob 런타임 저장소 (LocalFsBlobStore, D37) — 런타임 데이터라 커밋 제외
**/attachments/
# secrets
.env
*.pem
*.key
